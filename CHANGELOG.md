# Changelog

## 1.0.0

* Initial release
* Config-driven ad management via JSON (Firebase Remote Config + local asset fallback)
* Interstitial ads with click threshold support
* App Open ads (Splash + OnResume)
* Drop-in `BannerAdWidget` and `NativeAdWidget`
* Zero boilerplate setup
